package com.example.ProjectCal;

public interface CalculatorService {
    double calculateSquareRoot(double number);
    long calculateFactorial(int number);
    int calculateModulus(int dividend, int divisor);
}
